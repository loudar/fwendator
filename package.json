{
  "name": "fwendator",
  "private": true,
  "scripts": {
    "minify": "bun build serviceScripts/discord.js --minify --outfile=getFriends.min.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
